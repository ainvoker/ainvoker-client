import WorkspacePage from "../../../components/workspace/WorkspacePage"

const Models = () => (
  <WorkspacePage
    title="Models"
    description="Register, version, and route traffic across model deployments."
  />
)

export default Models
