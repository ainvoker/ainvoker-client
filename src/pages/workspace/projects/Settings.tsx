import WorkspacePage from "../../../components/workspace/WorkspacePage"

const ProjectSettings = () => (
  <WorkspacePage
    title="Settings"
    description="Project metadata, environment defaults, and retention policies."
  />
)

export default ProjectSettings
